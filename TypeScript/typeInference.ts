// type inference
// understanding type inference
// type annotation

let val = 12; // "val" type inference when we dont define type and it itself infere with value

// type annotaion---> where we define the type of varriable with colon :

let num1: number;
let value: number | string | boolean;
